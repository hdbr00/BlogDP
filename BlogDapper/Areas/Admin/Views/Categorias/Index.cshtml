@model IEnumerable<BlogDapper.Models.Categoria>

    @{
        ViewData["Title"] = "Lista de Categoria";
    }
<div class="row">
    <div class="col-9">
        <h1>Administrador de categorias</h1>
    </div>
    <div class="col-3">
        <a asp-action="Crear" class="btn btn-primary float-end"><i class="bi bi-plus-square"></i> Agregar Categoría</a>
    </div>
</div>
<div class="card bg-light p-3">
    <table class="table" id="tblCategorias">
        <thead>
        <tr>
            <th>Id</th>
            <th>Nombre</th>
            <th>Fecha de Cración</th>
            <th>Acciones</th>
        </tr>
        </thead>
     @*   <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.IdCategoria</td>
                    <td>@item.Nombre</td>
                    <td>@item.FechaCreacion</td>
                </tr>
            }
        </tbody>*@
    </table>
</div>

@section Scripts{
    <script src="~/js/categorias.js"></script>

}